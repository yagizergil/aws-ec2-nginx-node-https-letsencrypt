# aws-ec2-nginx-node-https
This project demonstrates a **production-grade Node.js API deployment** on AWS EC2 using **Nginx as a reverse proxy**, **HTTPS with Let’s Encrypt**, and a **secure network design**.
